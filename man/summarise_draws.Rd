% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_draws.R
\name{summarise_draws}
\alias{summarise_draws}
\alias{summarize_draws}
\title{Fast summaries of posterior draws}
\usage{
summarise_draws(x, stats = .posteriorcpp_default_stats)

summarize_draws(x, stats = .posteriorcpp_default_stats)
}
\arguments{
\item{x}{A \code{draws} object, or an object coercible to one via
\code{\link[posterior:as_draws]{posterior::as_draws()}}.}

\item{stats}{Character vector of statistics to compute. The default is
\code{"mean"}, \code{"median"}, \code{"sd"}, \code{"mad"}, \code{"q5"}, \code{"q95"}, \code{"rhat"},
\code{"ess_bulk"}, \code{"ess_tail"} — matching \code{posterior::summarise_draws()}'s
own default set exactly. More are available opt-in, each matching its
\code{posterior} equivalent: \code{"var"} (variance, i.e. \code{sd^2}); the unnormalised
diagnostics \code{"rhat_basic"}/\code{"ess_basic"} (split-chains without the
rank-normalising z-score step that \code{"rhat"}/\code{"ess_bulk"} apply);
\code{"ess_mean"} (identical to \code{"ess_basic"}, the ESS of the mean
estimator); \code{"ess_sd"} (ESS of the squared centered draws, i.e. of the
SD estimator); and the Monte Carlo standard errors \code{"mcse_mean"}
(\code{sd / sqrt(ess_mean)}) and \code{"mcse_sd"} (first-order Taylor
approximation per Kenney & Keeping 1951). Output columns are always in
the fixed canonical order above regardless of how \code{stats} is ordered.
Statistics that share underlying work (e.g. \code{"rhat"} and \code{"ess_bulk"}
both rank-normalise and split the chains; \code{"rhat_basic"} and \code{"rhat"}
both start from the same unnormalised split; \code{"mcse_sd"} reuses
\code{"ess_sd"}'s ESS as its own denominator) still only do that work once,
but requesting fewer statistics skips work that only unrequested
statistics need — e.g. \code{stats = c("mean", "sd")} skips sorting,
rank-normalisation, and FFT autocovariance entirely.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per variable and one
column per requested statistic.
}
\description{
A C++/RcppEigen implementation of \code{posterior::summarise_draws()} computing
the default summary and convergence measures: mean, median, sd, mad, q5,
q95, rhat, ess_bulk, and ess_tail. Additional measures matching their
\code{posterior} equivalents are available opt-in via \code{stats} (see below).
}
