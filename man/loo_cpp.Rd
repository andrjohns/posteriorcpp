% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{loo_cpp}
\alias{loo_cpp}
\title{Fast PSIS-LOO cross-validation}
\usage{
loo_cpp(x, r_eff = TRUE, save_psis = FALSE)
}
\arguments{
\item{x}{A log-likelihood array with dimensions iterations x chains x
observations, as returned by e.g.
\code{posterior::extract_variable_matrix()}-style \code{draws_array} objects.}

\item{r_eff}{Relative MCMC effective sample sizes of \code{exp(log_lik)} for
each observation, used to adjust the PSIS tail-length and diagnostics.
One of:
\itemize{
\item \code{TRUE} (the default): estimate \code{r_eff} from \code{x} inside the same pass
that computes PSIS-LOO, reusing the FFT-based ESS machinery instead of
the separate \code{\link[loo:relative_eff]{loo::relative_eff()}} traversal of the array that
\code{loo::loo()} callers otherwise need.
\item \code{FALSE} or \code{NULL}: skip the estimate and use \code{r_eff = 1} for every
observation, matching \code{loo::loo()}'s own default.
\item A single number or a numeric vector with one value per observation,
used as-is.
}}

\item{save_psis}{Should the smoothed, unnormalized PSIS log weights be
kept in the returned object as \code{psis_object}? Matches the \code{save_psis}
argument of \code{\link[loo:loo]{loo::loo()}}.}
}
\value{
An object with the same structure as \code{\link[loo:loo]{loo::loo()}}'s return value:
class \code{c("psis_loo", "importance_sampling_loo", "loo")}, with
\code{estimates}, \code{pointwise}, \code{diagnostics}, and (if \code{save_psis = TRUE})
\code{psis_object} components.
}
\description{
A C++/Eigen implementation of \code{loo::loo.array()}: approximate
leave-one-out cross-validation via Pareto-smoothed importance sampling
(PSIS).
}
